<%unless @transfer_details.nil? %>
<table class="table  table-striped"> 
  <tbody>
  <% @transfer_details.each do |t| %>
    <tr>
        <td><%= t.product.name %></td>
        <td><%= t.quantity.to_s + t.product.measurement %> </td>
    </tr>
  <% end %>
  </tbody>
</table>
<%end%>