<div class="span8">
  <%= render 'category' %>
  <% if @products.count > 0 %>
  <table class="table table-condensed">
    <thead>
      <tr>
        <th><%= t(:product_name) %></th>
        <th><%= t(:unit_price) %></th>
        <th></th>   
      </tr>
    </thead>   
    <tbody>
    <% @products.each do |product| %>
	  <%= render product %>
    <% end %>
    </tbody>
	<tr>
	  <td colspan="3">
			 
	  </td>
	</tr>
  </table>
  <%end%>
</div> 