<tr>
  <td><%= link_to product.name, product %></td>
  <td><%= product.price(current_user.store_id) %></td>
  <td><%= link_to t(:add_to_cart), new_cart_path(:product_id => product.id), remote:true, :class => "btn btn-mini btn-warning" %>
  </td>            
</tr>