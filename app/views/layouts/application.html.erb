<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : t(:app_name) %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : t(:app_name) %>">
    <%= stylesheet_link_tag "application", :media => "all" %>
    <%= javascript_include_tag "application"%>
    <%= csrf_meta_tags %>
    <%= yield(:head) %>
  </head>
  <body>
    <div class="navbar " style="margin-bottom: 5px;">
      <div class="navbar-inner">
        <%= link_to t(:app_name), root_path, :class => 'brand' %>
        <%= render 'layouts/navigation' %>
      </div>
    </div>
    <div id="main" role="main">
      <div class="container">
        <div class="content">
           <div class="row">
            <div class="span12 ">
              <%= render 'layouts/messages' %>
              <%= yield %>
            </div>
          </div>
          <footer>
          </footer>
        </div>
      </div> <!--! end of .container -->
    </div> <!--! end of #main -->
    <center>
    <footer><a href="http://www.miitbeian.gov.cn/"><%= t(:bei_an) %></a></footer>
    </center>
    <%= debug(params) if Rails.env.development? %>
  </body>
</html>