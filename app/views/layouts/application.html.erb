<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= content_for?(:title) ? yield(:title) : t(:app_name) %></title>
    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
    <%= yield(:head) %>
  </head>
  <body >
    <div id="navigation">
      <%= render 'layouts/navigation' %>
    </div>
    <div id="container" style="margin-left:0px;">
      <%= render 'layouts/messages' %>
      <%= yield %>
    </div>
    <div class="footer" style="margin-top:10px;border-top: solid 1px #3181C0;padding-top: 5px;">
      <center>
        <footer><a href="http://www.miitbeian.gov.cn/"><%= t(:bei_an) %></a></footer>
      </center>
    </div>
    <%= debug(params) if Rails.env.development? %>
    <%= javascript_include_tag "application" %>
  </body>
</html>