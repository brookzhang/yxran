<p>
	<span class="label label-info"><%= @product.name %></span>
	<%= @product.price(1) %>
</p>
<p><%= t(:description) %>: <%= @product.description %></p>
<p><%= t(:category) %>: 
	<%= link_to @product.category.name, maintain_products_path(:category_id => @product.category_id), :class => 'badge badge-warning' %>
</p>
<p><%= t(:created_at) %>: <%= @product.created_at.to_date %></p>

<%= render :partial => 'maintain/stocks/list_stores' %>

<p>
<%= link_to t(:back), maintain_products_path, :class => 'btn'%>
</p>