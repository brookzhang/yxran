<%= simple_form_for [:maintain,@transfer], :html => {:class => 'form-horizontal' } do |f| %>
  <%= f.error_notification %>
  <%= display_base_errors @transfer %>
  <%= f.input :product, :label => t(:product_name), :input_html => {:value => @transfer.product.name, :disabled => true } %>
  <%= f.input :from_store, :label => t(:from_store), :input_html => {:value => @transfer.from_store.name, :disabled => true } %> 
  <%= f.input :to_store, :label => t(:to_store), :input_html => {:value => @transfer.to_store.name, :disabled => true } %> 
  <%= f.input :quantity, :label => t(:quantity), :required => true %>
  <%= f.input :remark, :label => t(:remark) %>
  <div class="control-group">
    <div class="controls">
      <%= f.button :submit, t(:submit), :class => 'btn' %>
      <%= link_to t(:back), :back, :class => 'btn' %>
    </div>
  </div>
  
<% end %>

