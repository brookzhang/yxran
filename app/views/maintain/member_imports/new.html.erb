<h3><%=t(:import_excel)%></h3>



<%= form_for [:maintain, @member_import] do |f| %>
  <% if @member_import.errors.any? %>
    <div id="error_explanation">
      <h4><%= pluralize(@member_import.errors.count, "error") %> <%=t(:prohibited_from_completing)%> :</h4>
      <ul>
      <% @member_import.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.file_field :file, :class => 'btn' %>
  </div>
  <div class="buttons"><%= f.submit  t(:import) , :class => 'btn btn-warning'%></div>
<% end %>