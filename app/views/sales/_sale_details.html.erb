<%if !@sale_details.nil? %>
<table class="table  table-striped">
  <!--<thead>
    <tr>
      <th><%= t(:product_name) %></th>
      <th><%= t(:quantity) %></th>
      <th><%= t(:created_at) %></th>                              
    </tr>
  </thead>-->   
  <tbody>
  <% @sale_details.each do |d| %>
    <tr>
        <td><%= link_to d.product.name, d.product, :class => "badge badge-warning" %> <%= d.price%> </td>
        <td> * <%= d.quantity.to_s %> 
          = <%= d.amount.to_s + t(:currency) %>
        </td>                         
    </tr>
  <% end %>
  </tbody>
</table>
<%end%>