<div class="span8">
<table class="table table-condensed">
	  <thead>
      <tr>
          <th>id</th>
          <th><%= t(:actual_amount) %></th>
          <th><%= t(:sale_category) %></th>
          <th></th>                                      
      </tr>
  </thead>   
  <tbody>
  <% @sales.each do |sale| %>
    <tr>
        <td><%= link_to '#'+sale.id.to_s, sale %></td>
        <td><%= sale.actual_amount %></td>
        <td><%= l(sale.category,"sale_category") %></td>
        <td></td>                                       
    </tr>
  <% end %>
  <tr>
    <td colspan="5">
      <%= link_to t(:back), root_path, :class => 'btn'%>
    </td>
  </tr>
  </tbody>
</table>
</div> 