<tr>
  <td><%= link_to stock.product.name, stock.product, :class => 'label secondary' %> <%= stock.price %></td>
  <td><%= stock.quantity %> </td>
  <td><%= link_to t(:add_to_cart), new_cart_path(:product_id => stock.product.id), remote:true, :class => "button tiny", "data-reveal-id" => "addModal" %>
  </td>            
</tr>
 